<%= link_to 'Back', current_user %>

<%= form_tag user_pantry_ingredients_path(current_user) do -%>
  <%= select_tag :id, options_from_collection_for_select(Ingredient.all, :id, :name) %>
  <div><%= submit_tag 'Add Ingredient' %></div>
<% end -%>