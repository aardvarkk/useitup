<h1>Listing recipe_ingredients</h1>

<table>
  <tr>
    <th>Recipe</th>
    <th>Ingredient</th>
    <th>Measure type</th>
    <th>Qty</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @recipe_ingredients.each do |recipe_ingredient| %>
  <tr>
    <td><%= recipe_ingredient.recipe_id %></td>
    <td><%= recipe_ingredient.ingredient_id %></td>
    <td><%= recipe_ingredient.measure_type_id %></td>
    <td><%= recipe_ingredient.qty %></td>
    <td><%= link_to 'Show', recipe_ingredient %></td>
    <td><%= link_to 'Edit', edit_recipe_ingredient_path(recipe_ingredient) %></td>
    <td><%= link_to 'Destroy', recipe_ingredient, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Recipe ingredient', new_recipe_ingredient_path %>
